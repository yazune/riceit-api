# Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
#spring.datasource.url = jdbc:mysql://localhost:3306/riceitdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
#spring.datasource.username = riceituser
#spring.datasource.password = riceituser

 credentials for Heroku Cloud Ignite MYSQL Database
spring.datasoure.url = jdbc:mysql://b691cf0a186c5f:f6200ced@eu-cdbr-west-03.cleardb.net/heroku_124f34e757934f2?reconnect=true
spring.datasource.username = b691cf0a186c5f
spring.datasource.password = f6200ced

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto = update

# todo todo todo todo todo check if required
spring.jpa.hibernate.use-new-id-generator-mappings=true

# Hibernate Logging
#logging.level.org.hibernate.SQL = DEBUG

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode = ALWAYS

spring.datasource.hikari.max-lifetime = 600000 

spring.jpa.open-in-view = true

# Jwt Provider properties
app.jwtSecret = JWTSuperSecretKey
app.jwtExpirationInMs = 3600000



